import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    container:{
        backgroundColor: '#7ff2a0',
        alignItems: 'center', 
        justifyContent:'center',
        width: '100%',
        flex:1
    },
    txt:{
        fontSize: 30,
        color:'#fff'
    }

})

export default styles